<old>to start use: python3 fptrans.py <firmware_password>
to get the security-password:
sudo nvram -p</old>

IMPORTANT: just execute the script and type in your admin password.

use python 3.x to get correct results